---
/*
 * Three questions worth asking, each one hard to answer anywhere else: what a
 * year actually earned, what your own books give away about you, and the
 * proof-of-funds bundle a bank asks for.
 *
 * Nobody has to ask for transfers to be left out of income. Kassiber pairs them
 * on its own, so that belongs in the answer as something already done, never in
 * the question as an instruction: a demo where the user coaches the tool is a
 * demo about a tool that needs coaching.
 *
 * Deliberately not the tax filing. The section above it already argues that
 * case, and an agent that can only produce a tax form is a narrower thing than
 * this one is.
 *
 * An agent transcript, not a chatbot. The section claims agents are first
 * class, so the panel has to show the thing that makes them first class: the
 * command it ran, in the real CLI's real syntax, and what came back. Answers
 * that simply appear are how a product demo looks; work that is visible is how
 * an agent looks.
 *
 * Every command here is checked against the installed CLI, so anyone who has
 * it can paste one and watch it work. Guessed flags would be found out by
 * exactly the people this section is addressed to.
 *
 * Prose streams a word at a time and each command takes as long as its work
 * plausibly would: a year-wide rollup is slow, a single-row lookup is quick,
 * writing a workbook is slowest. One uniform pause for all three was what made
 * the old version read as a slideshow.
 */

const MONTHS = [
  { m: "J", incoming: 6, outgoing: 2, transfers: 4, swaps: 1 },
  { m: "F", incoming: 4, outgoing: 3, transfers: 3, swaps: 0 },
  { m: "M", incoming: 7, outgoing: 2, transfers: 8, swaps: 2 },
  { m: "A", incoming: 5, outgoing: 4, transfers: 5, swaps: 1 },
  { m: "M", incoming: 3, outgoing: 5, transfers: 4, swaps: 0 },
  { m: "J", incoming: 6, outgoing: 3, transfers: 6, swaps: 1 },
  { m: "J", incoming: 4, outgoing: 2, transfers: 5, swaps: 3 },
  { m: "A", incoming: 7, outgoing: 4, transfers: 7, swaps: 1 },
  { m: "S", incoming: 3, outgoing: 6, transfers: 4, swaps: 2 },
  { m: "O", incoming: 5, outgoing: 3, transfers: 6, swaps: 0 },
  { m: "N", incoming: 8, outgoing: 2, transfers: 8, swaps: 1 },
  { m: "D", incoming: 4, outgoing: 4, transfers: 5, swaps: 0 },
];

/* What it says back, split into words here rather than in the browser.
   Component CSS is scoped by an attribute Astro stamps onto template elements,
   and a span created by script never carries it, so the fade and the caret
   simply did not apply and the whole sentence appeared at once. Authoring the
   words also means the full answer is there with no JS at all. */
const SAY = {
  year:
    "Money in, money out, transfers, and swaps. Your moves between your own wallets were already paired off, so none of them count as income.",
  privacy:
    "Two of them. Reused addresses tie your history together, and merged inputs prove the wallets are one owner.",
  funds:
    "Every lot traced back to the day it arrived, with the exchange records attached. Hand them the file, not your wallets.",
};
const words = (s: string) => s.split(" ");

// Full month names for the chart's native hover tooltips.
const NAMES = ["Jan", "Feb", "Mar", "Apr", "May", "Jun", "Jul", "Aug", "Sep", "Oct", "Nov", "Dec"];

// Chart geometry mirrors the app's grouped transaction-overview bars.
const W = 320;
const H = 94;
const BASE = 62;
const DENSITY = 0.75;
const maxActivity = Math.max(...MONTHS.flatMap((x) => [x.incoming, x.outgoing, x.transfers, x.swaps]));
const slot = (W - 8) / MONTHS.length;
const bars = MONTHS.map((x, i) => {
  const incomingHeight = Math.max(2, (x.incoming / maxActivity) * (BASE - 12) * DENSITY);
  const outgoingHeight = Math.max(2, (x.outgoing / maxActivity) * 15 * DENSITY);
  const transferHeight = Math.max(2, (x.transfers / maxActivity) * (BASE - 12) * DENSITY);
  const swapHeight = x.swaps ? Math.max(2, (x.swaps / maxActivity) * (BASE - 12) * DENSITY) : 0;
  return {
    ...x,
    x: 5 + i * slot,
    incomingHeight,
    outgoingHeight,
    transferHeight,
    swapHeight,
    width: Math.max(2, (slot - 8) / 4),
  };
});
---

<div class="chat" data-turn="0">
  <!-- Which harness is holding the wheel. The privacy claim lives in each
       answer's provenance line, not up here. -->
  <div class="chat-head">
    <span class="lights" aria-hidden="true"><i></i><i></i><i></i></span>
    <span class="harness">opencode</span>
    <!-- In the bar rather than floating over it: its slot is held from the
         start, so arriving at the end moves nothing. -->
    <button class="replay" type="button" data-replay>Replay</button>
  </div>

  <!-- Every visual decision below mirrors ui-tauri's assistant thread
       (ChatMessage, ChatToolCall, tool.tsx): neutral rounded user bubbles,
       plain full-width assistant text, tool calls in a left rail under a
       "Tool usage" label, result facts as small labeled tiles, and a muted
       provenance line under each answer. The mock should look like a
       screenshot of the product, not like a website's idea of a chat. -->
  <div class="chat-log" data-log>
    <!-- ── 1 · the year at a glance ─────────────────────────── -->
    <div class="msg you" data-at="1">What did I actually earn last year?</div>

    <div class="msg bot" data-at="2">
      {/* Three commands, because the first one does not work. Imports have
          landed since the reports were last built, so the summary comes back
          stale and says which command fixes it. The agent runs that, then asks
          again. This is the documented behaviour rather than a dramatisation:
          a stale report names `journals process` as its own next step, which is
          what lets an agent recover without being told. */}
      <div class="tools">
        <span class="tools-label"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M14.7 6.3a1 1 0 0 0 0 1.4l1.6 1.6a1 1 0 0 0 1.4 0l3.77-3.77a6 6 0 0 1-7.94 7.94l-6.91 6.91a2.12 2.12 0 0 1-3-3l6.91-6.91a6 6 0 0 1 7.94-7.94l-3.76 3.76z"/></svg>Tool usage<svg class="chev" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="m6 9 6 6 6-6"/></svg></span>
        <div class="rail">
          <div class="tool" data-tool data-dur="700" data-fail data-out="Stale since your last import · run journals process">
            <div class="trow">
              <span class="tmark" aria-hidden="true"></span>
              <code>kassiber reports summary</code>
              <span class="tstate" aria-hidden="true"></span>
            </div>
            <div class="tout" data-tout></div>
          </div>

          <div class="tool" data-tool data-dur="1400" data-out="Rebuilt 1,284 transactions">
            <div class="trow">
              <span class="tmark" aria-hidden="true"></span>
              <code>kassiber journals process</code>
              <span class="tstate" aria-hidden="true"></span>
            </div>
            <div class="tout" data-tout></div>
          </div>

          <div class="tool" data-tool data-dur="800" data-out="1,284 transactions, 12 months">
            <div class="trow">
              <span class="tmark" aria-hidden="true"></span>
              <code>kassiber reports summary</code>
              <span class="tstate" aria-hidden="true"></span>
            </div>
            <div class="tout" data-tout></div>
          </div>
        </div>
      </div>

      <p data-stream>{words(SAY.year).map((w) => <span>{`${w} `}</span>)}</p>

      {/* The chart is the one payload the app would render as a widget, so it
          keeps a tile border. The prose above each payload is its caption. */}
      <div class="card tile" data-card>
        <svg
          class="spark"
          viewBox={`0 0 ${W} ${H}`}
          role="img"
          aria-label="Transaction activity by month in 2025, split into incoming, outgoing, transfers, and swaps"
        >
          <line x1="0" y1="12" x2={W} y2="12" class="grid" />
          <line x1="0" y1="42" x2={W} y2="42" class="grid" />
          <line x1="0" y1={BASE} x2={W} y2={BASE} class="axis" />
          {/* Delay per month, so the year fills in left to right rather than
              appearing at once. Each series is nudged a little further so the
              sweep has some depth instead of moving as one block.
              One group per month: hovering it lifts that month out of the year
              and the browser's own tooltip names the figures, so the chart
              rewards a second look after the show is over. */}
          {bars.map((b, i) => (
            <g class="mgrp">
              <title>{`${NAMES[i]} · ${b.incoming} in · ${b.outgoing} out · ${b.transfers} transfers${b.swaps ? ` · ${b.swaps} swap${b.swaps > 1 ? "s" : ""}` : ""}`}</title>
              <rect class="hit" x={b.x - 2} y="0" width={slot} height={H} />
              <rect x={b.x} y={BASE - b.incomingHeight} width={b.width} height={b.incomingHeight} rx="1.5" class="bar incoming" style={`--bd:${i * 38}ms`} />
              <rect x={b.x + b.width + 1} y={BASE} width={b.width} height={b.outgoingHeight} rx="1.5" class="bar outgoing" style={`--bd:${i * 38 + 20}ms`} />
              <rect x={b.x + (b.width + 1) * 2} y={BASE - b.transferHeight} width={b.width} height={b.transferHeight} rx="1.5" class="bar transfer" style={`--bd:${i * 38 + 40}ms`} />
              <rect x={b.x + (b.width + 1) * 3} y={BASE - b.swapHeight} width={b.width} height={b.swapHeight} rx="1.5" class="bar swap" style={`--bd:${i * 38 + 80}ms`} />
              <text x={b.x + slot / 2 - 2} y={H - 2} class="mo">{b.m}</text>
            </g>
          ))}
        </svg>
        <div class="legend">
          <span><i class="dot incoming"></i>Incoming</span>
          <span><i class="dot outgoing"></i>Outgoing</span>
          <span><i class="dot transfer"></i>Transfers</span>
          <span><i class="dot swap"></i>Swaps</span>
        </div>
      </div>

      {/* The line the app signs every answer with. It is the privacy claim in
          the product's own words, so it earns its place in all three turns. */}
      <p class="prov" data-prov>3 local tools · journals refreshed · local model</p>
    </div>

    <!-- ── 2 · what the books say about you ─────────────────── -->
    <div class="msg you" data-at="3">Which of my wallets leak the most about me?</div>

    <div class="msg bot" data-at="4">
      <div class="tools">
        <span class="tools-label"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M14.7 6.3a1 1 0 0 0 0 1.4l1.6 1.6a1 1 0 0 0 1.4 0l3.77-3.77a6 6 0 0 1-7.94 7.94l-6.91 6.91a2.12 2.12 0 0 1-3-3l6.91-6.91a6 6 0 0 1 7.94-7.94l-3.76 3.76z"/></svg>Tool usage<svg class="chev" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="m6 9 6 6 6-6"/></svg></span>
        <div class="rail">
          <div class="tool" data-tool data-dur="1100" data-out="3 things worth fixing">
            <div class="trow">
              <span class="tmark" aria-hidden="true"></span>
              <code>kassiber reports privacy-hygiene</code>
              <span class="tstate" aria-hidden="true"></span>
            </div>
            <div class="tout" data-tout></div>
          </div>
        </div>
      </div>

      <p data-stream>{words(SAY.privacy).map((w) => <span>{`${w} `}</span>)}</p>

      {/* Findings as the app's deterministic-fact tiles: uppercase source
          label over a plain figure, no surrounding card. */}
      <div class="card" data-card>
        <dl class="kv">
          <div><dt>Reused addresses</dt><dd class="hot" data-count="14" data-suffix=" outputs">14 outputs</dd></div>
          <div><dt>Merged inputs</dt><dd data-count="2" data-suffix=" wallets">2 wallets</dd></div>
          <div><dt>Round-number withdrawals</dt><dd data-count="6">6</dd></div>
        </dl>
      </div>

      <p class="prov" data-prov>1 local tool · local model</p>
    </div>

    <!-- ── 3 · the document a bank asks for, built here ─────── -->
    <div class="msg you" data-at="5">My bank wants proof of where the coins came from. Build it.</div>

    <div class="msg bot" data-at="6">
      <div class="tools">
        <span class="tools-label"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M14.7 6.3a1 1 0 0 0 0 1.4l1.6 1.6a1 1 0 0 0 1.4 0l3.77-3.77a6 6 0 0 1-7.94 7.94l-6.91 6.91a2.12 2.12 0 0 1-3-3l6.91-6.91a6 6 0 0 1 7.94-7.94l-3.76 3.76z"/></svg>Tool usage<svg class="chev" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="m6 9 6 6 6-6"/></svg></span>
        <div class="rail">
          <div class="tool" data-tool data-dur="2400" data-out="41 pages, ready to send">
            <div class="trow">
              <span class="tmark" aria-hidden="true"></span>
              <code>kassiber reports export-source-funds-bundle --file source-of-funds.zip</code>
              <span class="tstate" aria-hidden="true"></span>
            </div>
            <div class="tout" data-tout></div>
          </div>
        </div>
      </div>

      <p data-stream>{words(SAY.funds).map((w) => <span>{`${w} `}</span>)}</p>

      <div class="card tile fit" data-card>
        <div class="attach">
          <span class="clip" aria-hidden="true">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round"><path d="M21.4 11.05 12.25 20.2a5.5 5.5 0 0 1-7.78-7.78l8.49-8.48a3.67 3.67 0 1 1 5.18 5.18l-8.48 8.49a1.83 1.83 0 0 1-2.6-2.6l7.79-7.78"/></svg>
          </span>
          <span class="file">source-of-funds.zip</span>
          <span class="download-icon" aria-label="Download">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M12 3v12"/><path d="m7 10 5 5 5-5"/><path d="M5 21h14"/></svg>
          </span>
        </div>
      </div>

      <p class="prov" data-prov>1 local tool · local model</p>
    </div>

  </div>

  <!-- Where the question comes from. Typed rather than posted whole, because a
       prompt that simply exists reads as a script, and watching one being
       written is what makes the pane feel driven by someone. Decorative: the
       real messages live in the log above, so this stays empty without JS. -->
  <div class="composer" aria-hidden="true">
    <span class="cline" data-composer></span>
    <span class="csend" data-csend>
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 19V5M5 12l7-7 7 7"/></svg>
    </span>
  </div>
</div>

<script>
  const REDUCED = window.matchMedia("(prefers-reduced-motion: reduce)").matches;

  for (const chat of document.querySelectorAll<HTMLElement>(".chat")) {
    const log = chat.querySelector<HTMLElement>("[data-log]");
    if (!log) continue;
    const msgs = [...chat.querySelectorAll<HTMLElement>(".msg")];
    const tools = [...chat.querySelectorAll<HTMLElement>("[data-tool]")];
    const composer = chat.querySelector<HTMLElement>(".composer");
    const cline = chat.querySelector<HTMLElement>("[data-composer]");
    // Provenance lines reveal on the same beat as the payload they sign.
    const cards = [...chat.querySelectorAll<HTMLElement>("[data-card], [data-prov]")];
    /* The words are already spans, one per word, rendered by the component.
       Arming them is what hides them; every word keeps its place from the first
       frame and only its opacity changes, so nothing reflows as the sentence
       arrives. */
    const proses = new Map<HTMLElement, HTMLElement[]>();
    for (const p of chat.querySelectorAll<HTMLElement>("[data-stream]")) {
      p.classList.add("is-armed");
      proses.set(p, [...p.querySelectorAll<HTMLElement>("span")]);
    }

    const counters = [...chat.querySelectorAll<HTMLElement>("[data-count]")];
    const countText = (el: HTMLElement, v: number) =>
      v.toFixed(Number(el.dataset.decimals ?? 0)) + (el.dataset.suffix ?? "");

    /* Counts up to the real figure. A number that lands already finished is
       just text; one that climbs reads as something being worked out.
       On the same timers as everything else, not on animation frames: frames
       are throttled while the tab is in the background, which left the figure
       sitting on the zero it was reset to, and a stray frame loop outlived
       Replay. The last tick assigns the target exactly, so the number on screen
       is never a rounded approximation of itself. */
    const TICKS = 26;
    const countUp = (el: HTMLElement) => {
      const target = Number(el.dataset.count);
      if (!Number.isFinite(target)) return;
      let f = 0;
      const tick = () => {
        f += 1;
        const k = f / TICKS;
        el.textContent = countText(el, f >= TICKS ? target : target * (1 - Math.pow(1 - k, 3)));
        if (f < TICKS) later(tick, 32);
      };
      tick();
    };

    let timers: number[] = [];
    const later = (fn: () => void, ms: number) => {
      timers.push(window.setTimeout(fn, ms) as unknown as number);
    };
    const clear = () => {
      timers.forEach((t) => window.clearTimeout(t));
      timers = [];
    };

    // Follow the conversation, the way a chat pane does.
    const follow = () => log.scrollTo({ top: log.scrollHeight, behavior: "auto" });

    const reset = () => {
      composer?.classList.remove("is-typing", "is-sending");
      if (cline) cline.textContent = "";
      for (const m of msgs) m.classList.remove("is-in", "is-thinking");
      for (const t of tools) {
        t.classList.remove("is-run", "is-done", "is-fail");
        const out = t.querySelector<HTMLElement>("[data-tout]");
        if (out) out.textContent = "";
      }
      for (const g of chat.querySelectorAll<HTMLElement>(".tools")) g.classList.remove("has-run");
      for (const c of cards) c.classList.remove("is-in");
      for (const [p, spans] of proses) {
        p.classList.remove("is-live");
        for (const s of spans) s.classList.remove("on", "tip");
      }
      for (const el of counters) el.textContent = countText(el, 0);
      chat.dataset.turn = "0";
      // Instant, not smooth: a replay should start at the top, not sail there.
      log.scrollTo({ top: 0, behavior: "instant" });
    };

    // The end state, arrived at with no journey. Where reduced motion lives.
    const settle = () => {
      clear();
      reset();
      for (const m of msgs) m.classList.add("is-in");
      for (const t of tools) {
        t.classList.add(t.hasAttribute("data-fail") ? "is-fail" : "is-done");
        const out = t.querySelector<HTMLElement>("[data-tout]");
        if (out) out.textContent = t.dataset.out ?? "";
      }
      for (const g of chat.querySelectorAll<HTMLElement>(".tools")) g.classList.add("has-run");
      for (const c of cards) c.classList.add("is-in");
      for (const [p, spans] of proses) {
        p.classList.add("is-live");
        for (const s of spans) s.classList.add("on");
      }
      for (const el of counters) el.textContent = countText(el, Number(el.dataset.count));
      chat.dataset.turn = "6";
      follow();
    };

    /* A word at a time, because that is what generated text looks like. Fast
       enough to read as output rather than as a typewriter effect. */
    const stream = (p: HTMLElement, done: () => void) => {
      p.classList.add("is-live");
      const spans = proses.get(p) ?? [];
      let i = 0;
      const tick = () => {
        spans[i - 1]?.classList.remove("tip");
        const s = spans[i++];
        s?.classList.add("on", "tip");
        follow();
        if (i < spans.length) later(tick, 26);
        else {
          s?.classList.remove("tip");
          done();
        }
      };
      tick();
    };

    /* Typed into the composer a character at a time, then sent. Pace varies a
       little per character so it reads as someone writing rather than as a
       fixed-rate ticker. */
    const type = (text: string, done: () => void) => {
      if (!composer || !cline) { done(); return; }
      composer.classList.add("is-typing");
      cline.textContent = "";
      let i = 0;
      const tick = () => {
        cline.textContent = text.slice(0, ++i);
        if (i < text.length) later(tick, 12 + Math.round((i * 7919) % 17));
        else done();
      };
      tick();
    };

    // Clears the line and flashes the button, which is what "sent" looks like.
    const send = () => {
      if (!composer || !cline) return;
      composer.classList.remove("is-typing");
      composer.classList.add("is-sending");
      cline.textContent = "";
      later(() => composer.classList.remove("is-sending"), 260);
    };

    type Step = { wait: number; run: (next: () => void) => void };
    const runSeq = (steps: Step[], i = 0) => {
      if (i >= steps.length) return;
      later(() => steps[i].run(() => runSeq(steps, i + 1)), steps[i].wait);
    };

    const play = () => {
      clear();
      reset();

      const steps: Step[] = [];
      const asks = msgs.filter((m) => m.classList.contains("you"));

      asks.forEach((ask, k) => {
        const answer = msgs[msgs.indexOf(ask) + 1];
        const turnTools = answer ? [...answer.querySelectorAll<HTMLElement>("[data-tool]")] : [];
        const prose = answer?.querySelector<HTMLElement>("[data-stream]");
        const payloads = answer ? [...answer.querySelectorAll<HTMLElement>("[data-card], [data-prov]")] : [];

        // Written into the composer first, one character at a time.
        steps.push({ wait: k === 0 ? 420 : 700, run: (n) => type(ask.textContent ?? "", n) });
        // Then sent, which is where it becomes a message in the log.
        steps.push({ wait: 320, run: (n) => { send(); ask.classList.add("is-in"); follow(); n(); } });
        // It thinks before it reaches for a command: a blinking cursor with
        // nothing behind it yet, which is what deciding looks like.
        steps.push({ wait: 420, run: (n) => { answer?.classList.add("is-in", "is-thinking"); follow(); n(); } });

        /* Each command in turn, held for as long as its own work would take. A
           command that fails still reports, and the next one is the recovery.
           Commands appear one by one as they are run, never as a stack laid
           out in advance: an agent that shows its plan up front reads as a
           script, one that reaches for the next command only after the last
           one answered reads as something deciding. */
        turnTools.forEach((tool, t) => {
          const out = tool.querySelector<HTMLElement>("[data-tout]");
          steps.push({ wait: t === 0 ? 620 : 340, run: (n) => { if (t === 0) answer?.classList.remove("is-thinking"); tool.closest(".tools")?.classList.add("has-run"); tool.classList.add("is-run"); follow(); n(); } });
          steps.push({
            wait: Number(tool.dataset.dur ?? 900),
            run: (n) => {
              tool.classList.remove("is-run");
              tool.classList.add(tool.hasAttribute("data-fail") ? "is-fail" : "is-done");
              if (out) out.textContent = tool.dataset.out ?? "";
              follow();
              n();
            },
          });
        });

        steps.push({ wait: 280, run: (n) => (prose ? stream(prose, n) : n()) });
        // The payloads, and any figure on them, once the sentence introducing
        // them is out.
        steps.push({
          wait: 200,
          run: (n) => {
            for (const c of payloads) {
              c.classList.add("is-in");
              for (const el of c.querySelectorAll<HTMLElement>("[data-count]")) countUp(el);
            }
            follow();
            n();
          },
        });
        if (k === asks.length - 1) {
          steps.push({ wait: 0, run: (n) => { chat.dataset.turn = "6"; n(); } });
        }
      });

      runSeq(steps);
    };

    // Replay honours the OS motion setting too: for reduced motion it simply
    // restores the finished conversation instead of animating through it.
    chat.querySelector("[data-replay]")?.addEventListener("click", REDUCED ? settle : play);

    if (REDUCED) {
      settle();
      continue;
    }

    const io = new IntersectionObserver(
      (entries) => {
        if (entries.some((e) => e.isIntersecting)) {
          io.disconnect();
          play();
        }
      },
      { threshold: 0.3 },
    );
    io.observe(chat);
  }
</script>

<style>
  .chat {
    position: relative;
    border: 1px solid var(--border);
    border-radius: var(--radius);
    background: var(--bg-card);
    box-shadow: 0 24px 56px -28px rgba(0, 0, 0, 0.75);
    overflow: hidden;
  }

  /* A fixed pane the conversation scrolls inside, so three exchanges cost the
     section no more height than one. */
  .chat-log {
    display: flex;
    flex-direction: column;
    gap: 14px;
    height: 452px;
    overflow-y: auto;
    padding: 18px;
    /* Every arrival glides the pane down instead of snapping it: reset opts
       out explicitly, and streaming words never change scrollHeight because
       they are laid out before they fade in. */
    scroll-behavior: smooth;
    scrollbar-color: rgba(255, 255, 255, 0.14) transparent;
    scrollbar-width: thin;
  }

  .chat-log::-webkit-scrollbar { width: 6px; }

  .chat-log::-webkit-scrollbar-thumb {
    background: rgba(255, 255, 255, 0.14);
    border-radius: 999px;
  }

  /* ── Messages ─────────────────────────────────────────── */
  /* Not-yet-sent messages take no space at all: the pane has to grow as the
     conversation arrives, otherwise scrolling to the bottom lands past a stack
     of invisible ones and the chat looks empty. */
  .msg { display: none; }

  .msg.is-in {
    display: block;
    animation: msgIn 0.3s ease both;
  }

  @keyframes msgIn {
    from { opacity: 0; transform: translateY(8px) scale(0.985); }
    to { opacity: 1; transform: none; }
  }

  /* The beat between the question landing and the first command going out:
     the app's ChatLoader, a spinner in a muted pill with a label. While it
     shows, the turn has no visible children, so the pill is the whole turn. */
  .bot.is-in.is-thinking {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    width: fit-content;
    padding: 5px 12px 5px 10px;
    border-radius: 999px;
    background: rgba(255, 255, 255, 0.06);
  }

  .bot.is-thinking::before {
    content: "";
    flex: none;
    width: 12px;
    height: 12px;
    border-radius: 999px;
    border: 1.5px solid rgba(255, 255, 255, 0.16);
    border-top-color: var(--fg-muted);
    animation: spin 0.8s linear infinite;
  }

  .bot.is-thinking::after {
    content: "Generating";
    font-size: 12px;
    color: var(--fg-dim);
  }

  @keyframes spin {
    to { transform: rotate(360deg); }
  }

  /* The user's bubble exactly as the app draws it: neutral secondary fill,
     uniform 16px radius, no border, no tail, no accent. */
  .you {
    align-self: flex-end;
    max-width: 82%;
    padding: 10px 12px;
    border-radius: 16px;
    background: rgba(255, 255, 255, 0.08);
    color: var(--fg);
    font-size: 14px;
    line-height: 1.45;
    letter-spacing: -0.005em;
  }

  /* The assistant exactly as the app draws it: no avatar, no bubble, plain
     full-width text. The work products around it carry the structure. */
  .bot {
    align-self: stretch;
    max-width: 100%;
  }

  .bot p {
    font-size: 14px;
    line-height: 1.5;
    letter-spacing: -0.005em;
    color: var(--fg-muted);
    margin-bottom: 10px;
  }

  /* Words are all in the layout from the first frame and only fade in, so the
     paragraph never reflows while it arrives and the card below it never walks
     down the pane. Appending text did both.
     Only hidden once the script has taken charge: without it the sentence has
     to read normally rather than be invisible forever. */
  /* Armed prose stays out of the layout entirely until its stream starts:
     a paragraph of invisible words still takes up space, and that phantom
     gap under the loader was what made the thinking beat feel wrong. Once
     live, the words are all laid out before any of them shows, so nothing
     reflows while the sentence arrives. */
  [data-stream].is-armed { display: none; }

  [data-stream].is-live { display: block; }

  [data-stream].is-armed span {
    opacity: 0;
    filter: blur(4px);
    transition: opacity 240ms ease, filter 300ms ease;
  }

  [data-stream].is-armed span.on {
    opacity: 1;
    filter: none;
  }

  /* Rides the leading edge, so the eye has somewhere to sit while it fills. */
  [data-stream].is-armed span.tip::after {
    content: "";
    display: inline-block;
    width: 0.42em;
    height: 0.95em;
    margin-left: 2px;
    vertical-align: -0.12em;
    background: var(--fg-dim);
    animation: caret 1s steps(2, jump-none) infinite;
  }

  @keyframes caret {
    0%, 100% { opacity: 1; }
    50% { opacity: 0; }
  }

  /* ── Answer payloads ──────────────────────────────────── */
  /* Land after the sentence that introduces them, so the answer arrives in
     the order it was written rather than all at once. The base carries no
     chrome; only .tile draws the app's bordered widget frame. */
  .card { display: none; }

  .card.is-in {
    display: block;
    animation: msgIn 0.3s ease both;
  }

  .card.tile {
    border: 1px solid var(--border);
    border-radius: 8px;
    background: rgba(255, 255, 255, 0.02);
    padding: 12px 13px;
  }

  .card.fit { width: fit-content; }

  /* The line the app signs every answer with. */
  .prov { display: none; }

  .prov.is-in {
    display: block;
    margin: 8px 0 0;
    font-size: 11.5px;
    color: var(--fg-faint);
    animation: msgIn 0.3s ease both;
  }

  .download-icon {
    display: grid;
    flex: none;
    width: 22px;
    height: 22px;
    place-items: center;
    color: var(--fg-dim);
  }

  .download-icon svg {
    width: 14px;
    height: 14px;
  }

  /* One nod once the file exists: the arrow dips and flashes ready-green,
     which is all "yours to take" needs. */
  .card.is-in .download-icon {
    animation: dlPop 0.7s ease 0.9s 1;
  }

  @keyframes dlPop {
    0%, 100% { transform: none; color: var(--fg-dim); }
    40% { transform: translateY(2px) scale(1.15); color: var(--ok); }
  }

  /* The app's deterministic-fact tiles: a grid of small bordered tiles, each
     with an uppercase source label over a plain figure. */
  .kv {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(140px, 1fr));
    gap: 8px;
  }

  .kv > div {
    border: 1px solid var(--border);
    border-radius: 8px;
    background: rgba(255, 255, 255, 0.02);
    padding: 8px 11px 9px;
  }

  .kv dt {
    margin-bottom: 3px;
    font-size: 10px;
    font-weight: 500;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    color: var(--fg-dim);
  }

  .kv dd {
    font-size: 14px;
    font-weight: 500;
    color: var(--fg);
    font-variant-numeric: tabular-nums;
  }

  .kv dd.hot { color: var(--warn); }

  .file {
    font-family: var(--font-mono);
    color: var(--fg-muted);
  }

  /* ── Monthly chart ────────────────────────────────────── */
  .spark {
    width: 100%;
    height: auto;
    display: block;
  }

  .grid {
    stroke: rgba(255, 255, 255, 0.055);
    stroke-width: 1;
  }

  .axis {
    stroke: rgba(255, 255, 255, 0.28);
    stroke-width: 1.5;
  }

  /* After the show, the chart still answers questions: hovering a month lifts
     it out of the year, and the group's <title> names its figures. */
  .mgrp { transition: opacity 160ms ease; }

  .spark:hover .mgrp:not(:hover) { opacity: 0.35; }

  .hit { fill: transparent; }

  .bar.incoming { fill: oklch(0.56 0.16 150); }
  .bar.outgoing { fill: var(--accent); }
  .bar.transfer { fill: oklch(0.56 0.04 260); }
  .bar.swap { fill: oklch(0.62 0.16 246); }

  /* Grown from the axis, the way the app draws them: money above the line,
     spending below it, so each bar rises or drops from where zero is. */
  .bar {
    transform-box: fill-box;
    transform-origin: 50% 100%;
  }

  .bar.outgoing { transform-origin: 50% 0%; }

  .card.is-in .bar {
    animation: barGrow 560ms cubic-bezier(0.2, 0.8, 0.2, 1) var(--bd, 0ms) both;
  }

  @keyframes barGrow {
    from { transform: scaleY(0); }
    to { transform: scaleY(1); }
  }

  /* Rows land one after another rather than as a finished table. */
  .card.is-in .kv > div {
    animation: msgIn 360ms ease both;
  }

  .card.is-in .kv > div:nth-child(1) { animation-delay: 90ms; }
  .card.is-in .kv > div:nth-child(2) { animation-delay: 200ms; }
  .card.is-in .kv > div:nth-child(3) { animation-delay: 310ms; }

  .mo {
    font-family: var(--font-mono);
    font-size: 7px;
    fill: var(--fg-faint);
    text-anchor: middle;
  }

  .legend {
    display: flex;
    flex-wrap: wrap;
    gap: 6px 14px;
    margin-top: 8px;
    font-size: 12px;
    color: var(--fg-dim);
  }

  .dot {
    display: inline-block;
    width: 7px;
    height: 7px;
    border-radius: 2px;
    margin-right: 6px;
  }

  .dot.incoming { background: oklch(0.56 0.16 150); }
  .dot.outgoing { background: var(--accent); }
  .dot.transfer { background: oklch(0.56 0.04 260); }
  .dot.swap { background: oklch(0.62 0.16 246); }

  /* ── File attachment ──────────────────────────────────── */
  .attach {
    display: flex;
    align-items: center;
    gap: 8px;
    font-size: 12px;
  }

  .clip {
    display: grid;
    place-items: center;
    width: 20px;
    height: 20px;
    flex: none;
    border-radius: 5px;
    background: rgba(255, 255, 255, 0.06);
    color: var(--fg-dim);
  }

  .clip svg { width: 12px; height: 12px; }

  .attach .file {
    flex: 1;
    min-width: 0;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
    font-size: 11.5px;
  }

  /* ── Harness and model ────────────────────────────────── */
  /* Lights left, harness centred, replay right — the same window chrome as
     the open-source section's terminal. Equal 1fr flanks keep the title on
     the true centre line. */
  .chat-head {
    display: grid;
    grid-template-columns: 1fr auto 1fr;
    align-items: center;
    gap: 12px;
    padding: 9px 14px;
    border-bottom: 1px solid var(--border);
    background: rgba(255, 255, 255, 0.02);
    font-family: var(--font-mono);
    font-size: 11px;
    color: var(--fg-dim);
  }

  .lights {
    display: flex;
    gap: 6px;
    justify-self: start;
  }

  /* Greyed out, matching the open-source section's terminal below. */
  .lights i {
    width: 10px;
    height: 10px;
    border-radius: 999px;
    background: #2a2a30;
  }

  .chat-head .harness { color: var(--fg-muted); }

  /* ── Tool calls ───────────────────────────────────────── */
  /* The app's AssistantRound: a "Tool usage" label, then every call in a
     left-ruled rail. The group only exists once its first command runs —
     idle grey blocks waiting their turn were the tell that the whole
     exchange was scripted. */
  .tools { display: none; }

  /* A class set by the script rather than :has(), which older engines would
     silently drop — leaving the agent's work invisible forever. */
  .tools.has-run {
    display: block;
    margin-bottom: 10px;
    animation: msgIn 0.3s ease both;
  }

  /* The app's ChainOfThoughtHeader: wrench, label, and the chevron of an
     opened disclosure, all on one line. */
  .tools-label {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    margin-bottom: 5px;
    font-size: 12px;
    font-weight: 500;
    color: var(--fg-dim);
  }

  .tools-label svg {
    width: 12px;
    height: 12px;
    flex: none;
  }

  .tools-label .chev {
    color: var(--fg-faint);
    transform: rotate(180deg);
  }

  .rail {
    display: flex;
    flex-direction: column;
    gap: 6px;
    border-left: 1px solid var(--border);
    padding-left: 10px;
  }

  .tool { display: none; }

  .tool.is-run,
  .tool.is-done,
  .tool.is-fail {
    display: block;
    animation: msgIn 0.3s ease both;
  }

  /* The app's Tool element: rounded-md, muted fill, mono command in the
     header row. One line, truncated with an ellipsis, exactly as the app's
     ToolHeader draws it — mark, command, and badge stay level. */
  .tool {
    border: 1px solid var(--border);
    border-radius: 6px;
    background: rgba(255, 255, 255, 0.02);
    overflow: hidden;
  }

  .trow {
    display: flex;
    align-items: center;
    gap: 8px;
    padding: 6px 9px;
  }

  .trow code {
    flex: 1;
    min-width: 0;
    font-family: var(--font-mono);
    font-size: 11px;
    line-height: 1.5;
    color: var(--fg-muted);
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }

  /* State icon: a ring while the command is out, a check or a warning after. */
  .tmark {
    display: grid;
    place-items: center;
    flex: none;
    width: 12px;
    height: 12px;
    border-radius: 999px;
    font-size: 10px;
    line-height: 1;
  }

  .tool.is-run .tmark {
    border: 1.5px solid rgba(255, 255, 255, 0.16);
    border-top-color: var(--fg-muted);
    animation: spin 0.8s linear infinite;
  }

  .tool.is-done .tmark::before {
    content: "✓";
    color: var(--ok);
  }

  .tool.is-fail .tmark::before {
    content: "!";
    color: var(--warn);
    font-weight: 700;
  }

  /* Uppercase state chip, the app's ToolHeader badge. */
  .tstate {
    flex: none;
    padding: 1px 6px;
    border-radius: 4px;
    background: rgba(255, 255, 255, 0.06);
    font-size: 9px;
    font-weight: 600;
    letter-spacing: 0.07em;
    text-transform: uppercase;
    color: var(--fg-dim);
  }

  .tool.is-run .tstate::before { content: "running"; }
  .tool.is-done .tstate::before { content: "done"; }
  .tool.is-fail .tstate::before { content: "stale"; }

  /* Not an alarm. The command did its job by refusing and naming the fix, so
     it reads as a step that still has to happen rather than as a failure. */
  .tool.is-fail .tstate {
    background: color-mix(in oklab, var(--warn) 14%, transparent);
    color: var(--warn);
  }

  .tool.is-fail .tout { color: var(--warn); }

  /* Below the rule: what came back, so the command and its result are one
     object rather than two stray lines. */
  .tout {
    padding: 6px 9px;
    border-top: 1px solid var(--border);
    background: rgba(255, 255, 255, 0.02);
    font-family: var(--font-sans);
    font-size: 12px;
    color: var(--fg-dim);
  }

  .tout:empty { display: none; }

  /* ── Replay ───────────────────────────────────────────── */
  /* Its slot in the bar is held from the start, so arriving at the end moves
     nothing. Unreachable until then, rather than merely invisible. */
  .replay {
    justify-self: end;
    font-family: var(--font-mono);
    font-size: 11px;
    color: var(--fg-faint);
    padding: 2px 7px;
    border-radius: 6px;
    border: 1px solid var(--border);
    background: transparent;
    cursor: pointer;
    opacity: 0;
    pointer-events: none;
    transition: opacity 0.25s ease, color 0.18s ease;
  }

  .replay:hover { color: var(--fg); }

  [data-turn="6"] .replay { opacity: 1; pointer-events: auto; }

  /* ── Composer ─────────────────────────────────────────── */
  .composer {
    display: flex;
    align-items: center;
    gap: 10px;
    padding: 10px 12px;
    border-top: 1px solid var(--border);
    background: rgba(255, 255, 255, 0.02);
  }

  /* An input field, not a caption: the pill is what makes the bottom edge read
     as the place messages come from. */
  .cline {
    flex: 1;
    min-width: 0;
    padding: 7px 13px;
    border: 1px solid var(--border);
    border-radius: 999px;
    background: rgba(255, 255, 255, 0.04);
    font-size: 13.5px;
    line-height: 1.4;
    color: var(--fg);
  }

  .cline:empty::before {
    content: "Ask anything";
    color: var(--fg-faint);
  }

  /* The caret sits on the line itself while it fills, so it tracks the text. */
  .composer.is-typing .cline::after {
    content: "";
    display: inline-block;
    width: 1.5px;
    height: 1em;
    margin-left: 1px;
    vertical-align: -0.15em;
    background: var(--fg);
    animation: caret 1s steps(2, jump-none) infinite;
  }

  .csend {
    display: grid;
    flex: none;
    place-items: center;
    width: 26px;
    height: 26px;
    border-radius: 999px;
    background: rgba(255, 255, 255, 0.06);
    color: var(--fg-dim);
    transition: background 160ms ease, color 160ms ease, transform 160ms ease;
  }

  .csend svg { width: 13px; height: 13px; }

  /* Live once there is something to send, and a beat as it goes. */
  .composer.is-typing .csend {
    background: rgba(255, 255, 255, 0.14);
    color: var(--fg);
  }

  .composer.is-sending .csend {
    background: var(--ok);
    color: #06120c;
    transform: translateY(-1px) scale(1.06);
  }

  @media (max-width: 450px) {
    .chat-log {
      height: 420px;
      overflow-y: hidden;
      padding: 14px;
    }

    .cline { font-size: 13px; }
  }


  /* The script settles straight to the end state here, so everything that only
     exists to show arrival is switched off rather than left mid-flight. */
  @media (prefers-reduced-motion: reduce) {
    .chat-log { scroll-behavior: auto; }
    .msg.is-in,
    .card.is-in,
    .card.is-in .bar,
    .card.is-in .kv > div,
    .card.is-in .download-icon,
    .prov.is-in,
    .tools.has-run,
    .tool.is-run,
    .tool.is-done,
    .tool.is-fail { animation: none; }
    .mgrp { transition: none; }
    [data-stream] span { opacity: 1; filter: none; transition: none; }
    [data-stream].is-armed span.tip::after,
    .composer.is-typing .cline::after { display: none; }
    .tool.is-run .tmark,
    .bot.is-thinking::before { animation: none; }
    /* The pane settles on the last turn, and phones cannot scroll it back.
       With no motion to contain, the cap has no job: let the whole
       conversation stand open so all of it can be read. */
    @media (max-width: 450px) {
      .chat-log {
        height: auto;
        overflow-y: visible;
      }
    }
    /* Nothing is ever typed here, so the prompt stays as the resting hint. */
    .csend { transition: none; }
  }
</style>
